bundle:
  name: mlops-demo-health-pressure-risk

variables:
  catalog:
    default: ds_dev_db
  schema:
    default: dev_christian_van_bellen
  cluster_id:
    description: "ID do cluster ML a usar nos Jobs (DBR 16.x ML)"
    default: ""

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-bacd151e-e2f8.cloud.databricks.com
    variables:
      catalog: ds_dev_db
      schema:  dev_christian_van_bellen

  prod:
    mode: production
    workspace:
      host: https://dbc-bacd151e-e2f8.cloud.databricks.com
    variables:
      catalog: ds_dev_db
      schema:  dev_christian_van_bellen

include:
  - resources/*.yml
